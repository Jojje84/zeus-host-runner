name: actions-register

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/actions-register.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - name: Register GitHub Actions
        run: echo "GitHub Actions registration check"
